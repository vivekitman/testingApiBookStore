# NCBC-style Angular Frontend

This Angular app consumes the ASP.NET Core API from `APIHindi` and renders:
- Hero banners
- Chairman message
- Latest announcements
- Quick links

## Run

```bash
npm install
npm start
```

The app expects API at `https://localhost:7216/api` by default.
